"""The speaker — how the engine's plan becomes the words the caller hears.

`context_card.py` renders the engine's current truth for the LLM, `history.py` keeps the
message window honest. The speaker never decides (D-02): it words `plan.say`.
"""
