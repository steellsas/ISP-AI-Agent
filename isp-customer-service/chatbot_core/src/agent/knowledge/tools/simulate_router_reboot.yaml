# Demo only: reflects what the caller physically did on the seeded line (router_reboot).
tool: simulate_router_reboot
capability: simulate
adapter: demo_db
args: { customer_id: str }
timeout_s: 2
on_failure:
  fallback: skip
