# ISP Support Agent Configuration

agent:
  company_name: "ISP Lietuva"
  agent_name: "Andrius"
  language: "lt"

greeting:
  template: "Labas, čia {company_name} klientų aptarnavimas. Mano vardas {agent_name}. Kuo galiu jums padėti?"

conversation:
  max_troubleshooting_attempts: 3
  timeout_seconds: 300

llm:
  model: "gpt-4o-mini"
  temperature: 0.3
  max_tokens: 500